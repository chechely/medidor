declare global {
    namespace Express {
        interface Request {
            flash: (event: string, message: string) => Promise<void>;
            consumeFlash: (event: string) => Promise<string[]>;
        }
    }
}
import { Request, Response, NextFunction } from 'express';
export interface FlashOption {
    sessionKeyName?: string;
    useCookieSession?: boolean;
}
export declare const flash: (options?: FlashOption) => (req: Request, response: Response, next: NextFunction) => void;
